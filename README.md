# せどり利益計算ツール（MVP）

仕入れ価格・販売価格などを入力するだけで、利益・利益率・ROI・仕入れ上限価格を計算するスマホ向けWebアプリです。

## 起動方法

npm install
npm run dev

ブラウザで http://localhost:3000 を開いてください。

## 計算ロジック

`lib/calc.ts` に計算ロジックをまとめています。UIから独立しているので、ロジックだけを見直したいときはこのファイルを確認してください。
